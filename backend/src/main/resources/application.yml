spring:
  config:
    import: optional:classpath:application-local.yml
  datasource:
    url: jdbc:postgresql://ep-late-meadow-ax7d9inr.c-4.us-east-2.aws.neon.tech/neondb?sslmode=require
    username: neondb_owner
    password: npg_2eUWASoqM7tP
  jpa:
    hibernate:
      # Override validate — Flyway owns the schema; ddl-auto: validate
      # fails on citext/timestamptz column types reported by Postgres metadata
      ddl-auto: none

app:
  jwt:
    secret: dzFiY/nlhxz9ufz5ETt8GluUFHlrEnZcB1XtYTuT09guhazFPuxTJfVawR3f4BYmXhV1asxwutUc1zjX5eoB3g==
  cors:
    allowed-origins: http://localhost:5180,http://localhost:5173,http://localhost:3000
  base-url: http://localhost:5180
  attendance:
    regularization:
      # N-day lookback window for regularization requests. OPEN QUESTION FOR PRODUCT
      # OWNER: is 30 days correct, and should it vary by role/department? Kept
      # configurable here rather than hardcoded pending that decision.
      lookback-days: 30

resend:
  api-key: ${RESEND_API_KEY:re_placeholder_set_RESEND_API_KEY_env_var}
  from: OneHR <onboarding@nforceone.com>

server:
  port: 8081

logging:
  level:
    com.nforce.onehr: DEBUG
    org.flywaydb: DEBUG
